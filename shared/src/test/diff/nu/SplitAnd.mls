:NewParser

fun f(x, y) =
  if x == 0 and
    y == 0 then "bruh"
    y == 1 then "lol"
//│ f: (anything, number,) -> ("bruh" | "lol")
//│  = [Function: f]

class A()
class B()
//│ Defined class A
//│ Defined class B
//│ A: () -> A
//│  = [Function: A1]
//│ B: () -> B
//│  = [Function: B1]

fun f(x) =
  if x == 0 and
    x is
      A() then "A"
      B() then "B"
    x == 0 then "lol"
    else "bruh"
//│ f: (A | B | number) -> ("A" | "B" | "bruh" | "lol")
//│  = [Function: f1]

fun f(x, y) =
  if
    x == 0 and
      y == 0 then "bruh"
      else "lol"
//│ f: (anything, number,) -> ("bruh" | "lol")
//│  = [Function: f2]
