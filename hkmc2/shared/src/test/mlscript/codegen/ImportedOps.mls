:js


module M with
  fun (~) concat(a, b) = a + b

open M

:sjs
fun foo() =
  "a" ~ "b" ~ "c"
foo()
//│ JS:
//│ function foo(...args) {
//│   globalThis.Predef.checkArgs("foo", 0, true, args.length);
//│   let tmp;
//│   tmp = globalThis.M.concat("a", "b") ?? null;
//│   return globalThis.M.concat(tmp, "c") ?? null;
//│ }
//│ this.foo() ?? null
//│ = 'abc'

let name = "_"
//│ name = '_'

let display(balance) = balance
//│ display = [Function (anonymous)]

"b" ~ display("-")
//│ = 'b-'


