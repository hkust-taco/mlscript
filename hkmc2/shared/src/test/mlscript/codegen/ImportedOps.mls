:js


module M with
  fun (~) concat(a, b) = a + b

open M

:sjs
fun foo() =
  "a" ~ "b" ~ "c"
foo()
//│ ————————————| JS (unsanitized) |————————————————————————————————————————————————————————————————————
//│ let foo, tmp;
//│ foo = function foo() {
//│   let tmp1;
//│   tmp1 = M1.concat("a", "b");
//│   return M1.concat(tmp1, "c")
//│ };
//│ tmp = M1.concat("a", "b");
//│ return M1.concat(tmp, "c")
//│ —————————————————| Output |—————————————————————————————————————————————————————————————————————————
//│ = "abc"

let name = "_"
//│ name = "_"

let display(balance) = balance
//│ display = fun display

"b" ~ display("-")
//│ = "b-"


