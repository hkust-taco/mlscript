:js

:sjs
fun fib(a) = if
  a <= 1 then a
  else fib(a - 1) + fib(a - 2)
//│ JS:
//│ function fib(...args) {
//│   globalThis.Predef.checkArgs("fib", 1, true, args.length);
//│   let a = args[0];
//│   let scrut, tmp, tmp1, tmp2, tmp3;
//│   scrut = a <= 1;
//│   if (scrut) {
//│     return a;
//│   } else {
//│     tmp = a - 1;
//│     tmp1 = globalThis.fib(tmp) ?? null;
//│     tmp2 = a - 2;
//│     tmp3 = globalThis.fib(tmp2) ?? null;
//│     return tmp1 + tmp3;
//│   }
//│ }
//│ undefined

fun f(x) = g(x)
fun g(x) = h(x)
fun h(x) = fib(x)
f(5)
//│ = 5
