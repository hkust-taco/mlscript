:js
:llir
:cpp

:intl
:sllir
:scpp
:todo
fun mkTup(x, y) = [x, y]
fun fst(t) = if t is [x, y] then x 
mkTup(1, 2)
//│ = [1, 2]
//│ ═══[COMPILATION ERROR] Unsupported kind of Call Call(Select(Select(Ref($runtime),Ident(Tuple)),Ident(get)),List(Arg(false,Ref(t),true), Arg(false,Lit(IntLit(0)),true)))
//│ Stopped due to an error during the Llir generation
