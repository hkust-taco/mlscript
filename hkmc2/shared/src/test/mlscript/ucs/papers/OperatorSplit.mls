:js

fun foo(x) = true

// :pt
// :elt
// :ucs normalized
:todo
fun example(x) =
  if Math.sign(x)
    < 0 then "invalid"
    == 0 then "null"
    |> Math.abs
      > 100 then "large"
      < 10 then "small"
      else "medium"

example(0)
//│ = "null"

example(-24)
//│ = "invalid"

:fixme
:expect "large"
example(200)
//│ ═══[RUNTIME ERROR] Expected: '"large"', got: '"small"'
//│ = "small"

example(5)
//│ = "small"

:fixme
:expect "medium"
example(79)
//│ ═══[RUNTIME ERROR] Expected: '"medium"', got: '"small"'
//│ = "small"
