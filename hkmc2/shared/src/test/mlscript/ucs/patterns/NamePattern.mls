:js

fun id(x) = x

:ucs desugared
if id(0) is t then t
//│ The simple split before desugaring:
//│ >  if
//│ >    let $scrut = member:id(0)
//│ >    $scrut is t then t
//│ Split expanded from the simple split:
//│ >  if
//│ >    let $scrut = member:id(0)
//│ >    let t = $scrut
//│ >    else t
//│ = 0
