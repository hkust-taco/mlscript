:js


:sjs
fun nonsense(xs) = if xs is
  [..ys] then ys
  [] then "empty"
//│ ————————————| JS (unsanitized) |————————————————————————————————————————————————————————————————————
//│ let nonsense;
//│ nonsense = function nonsense(xs) {
//│   if (runtime.Tuple.isArrayLike(xs) && xs.length >= 0) {
//│     let tmp;
//│     tmp = xs.length - 0;
//│     return runtime.safeCall(xs.slice(0, tmp))
//│   }
//│   throw globalThis.Object.freeze(new globalThis.Error("match error"));
//│ };
//│ —————————————————| Output |—————————————————————————————————————————————————————————————————————————

nonsense([])
//│ = []

nonsense([1, 2, 3, 4])
//│ = [1, 2, 3, 4]

:sjs
fun lead_and_last(xs) = if xs is
  [x, ..ys, y] then x + y
  [] then 0
//│ ————————————| JS (unsanitized) |————————————————————————————————————————————————————————————————————
//│ let lead_and_last;
//│ lead_and_last = function lead_and_last(xs) {
//│   let lastElement1$, element0$;
//│   if (runtime.Tuple.isArrayLike(xs) && xs.length >= 2) {
//│     let tmp2;
//│     element0$ = runtime.Tuple.get(xs, 0);
//│     tmp2 = xs.length - 1;
//│     runtime.safeCall(xs.slice(1, tmp2));
//│     lastElement1$ = runtime.Tuple.get(xs, -1);
//│     return element0$ + lastElement1$
//│   } else if (runtime.Tuple.isArrayLike(xs) && xs.length === 0) {
//│     return 0
//│   }
//│   throw globalThis.Object.freeze(new globalThis.Error("match error"));
//│ };
//│ —————————————————| Output |—————————————————————————————————————————————————————————————————————————

lead_and_last(["foo", "bar"])
//│ = "foobar"

lead_and_last([1, 2, 3, 4])
//│ = 5

lead_and_last([])
//│ = 0

:re
lead_and_last(["boom"])
//│ ═══[RUNTIME ERROR] Error: match error

:sjs
fun nested_tuple_patterns(xs) = if xs is
  [x, ..[y, z], w] then x + y + z + w
  [] then 0
//│ ————————————| JS (unsanitized) |————————————————————————————————————————————————————————————————————
//│ let nested_tuple_patterns;
//│ nested_tuple_patterns = function nested_tuple_patterns(xs) {
//│   let lastElement1$, element0$, element1$, element0$1, tmp6, tmp7;
//│   if (runtime.Tuple.isArrayLike(xs) && xs.length >= 2) {
//│     let inlinedVal, tmp8;
//│     element0$ = runtime.Tuple.get(xs, 0);
//│     tmp8 = xs.length - 1;
//│     inlinedVal = runtime.safeCall(xs.slice(1, tmp8));
//│     lastElement1$ = runtime.Tuple.get(xs, -1);
//│     if (runtime.Tuple.isArrayLike(inlinedVal) && inlinedVal.length === 2) {
//│       element0$1 = runtime.Tuple.get(inlinedVal, 0);
//│       element1$ = runtime.Tuple.get(inlinedVal, 1);
//│       tmp6 = element0$ + element0$1;
//│       tmp7 = tmp6 + element1$;
//│       return tmp7 + lastElement1$
//│     }
//│     throw globalThis.Object.freeze(new globalThis.Error("match error"));
//│   } else if (runtime.Tuple.isArrayLike(xs) && xs.length === 0) {
//│     return 0
//│   }
//│   throw globalThis.Object.freeze(new globalThis.Error("match error"));
//│ };
//│ —————————————————| Output |—————————————————————————————————————————————————————————————————————————

nested_tuple_patterns of [1, 2, 3, 4]
//│ = 10

fun hack(tupleSlice) = if tupleSlice is
  [..tupleGet, x] then x

:expect 4
hack([1, 2, 3, 4])
//│ = 4
