

:ucs desugared
// * Note that this always associates to the left
fun f(x) =
  if x ==
    1 +
      2 then 0
      _ then 1
//│ Desugared:
//│ >  if
//│ >    let $scrut = builtin:==(x, 1)
//│ >    let $scrut = builtin:+($scrut, 2)
//│ >    $scrut is true then 0
//│ >    else 1

