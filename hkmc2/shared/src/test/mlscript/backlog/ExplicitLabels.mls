:js


do exit:
  if true do exit.break(42)
  0
//│ = 42


let i = 0 in do loop:
  if i >= 3 do loop.break(i)
  set i += 1
  loop.continue
//│ = 3


let i = 0 in do outLoop:
  do inner:
    if i >= 4 do outLoop.break(i)
    set i += 1
    outLoop.continue
//│ = 4


:e
missing.break(1)
//│ ╔══[COMPILATION ERROR] Name not found: missing
//│ ║  l.26: 	missing.break(1)
//│ ╙──      	^^^^^^^
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.


:e
missing.continue
//│ ╔══[COMPILATION ERROR] Name not found: missing
//│ ║  l.34: 	missing.continue
//│ ╙──      	^^^^^^^
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.


:e
do lbl:
  lbl.continue(1)
//│ ╔══[COMPILATION ERROR] Label continue does not take arguments.
//│ ║  l.43: 	  lbl.continue(1)
//│ ╙──      	  ^^^^^^^^^^^^^^^
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.


:e
do lbl:
  lbl.break(1, 2)
//│ ╔══[COMPILATION ERROR] Label break expects at most one argument.
//│ ║  l.52: 	  lbl.break(1, 2)
//│ ╙──      	  ^^^^^^^^^^^^^^^


:e
lbl:
  lbl.break(1)
//│ ╔══[COMPILATION ERROR] Name not found: lbl
//│ ║  l.60: 	  lbl.break(1)
//│ ╙──      	  ^^^
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.
//│ lbl = undefined


let i = 0 in let total = 0 in do exitLbl:
  if i >= 3 do exitLbl.break(total)
  let j = 0
  do inner:
    if j >= 3 do inner.break(())
    if j == 1 do inner.break(())
    set total += i * 10 + j
    set j += 1
    inner.continue
  set i += 1
  exitLbl.continue
//│ = 30


let i = 0 in let steps = 0 in do done:
  while true do
    set i += 1
    let j = 0
    while true do
      set steps += 1
      if j == 1 do done.break(i * 100 + steps)
      set j += 1
//│ = 102


:e
do root:
  fun escape() = root.break(42)
  escape()
//│ ╔══[COMPILATION ERROR] Non-local label breaks are only supported with effect handlers enabled.
//│ ║  l.95: 	  fun escape() = root.break(42)
//│ ╙──      	                 ^^^^
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.


:e
do root:
  let escape = () => root.continue
  escape()
//│ ╔══[COMPILATION ERROR] Label continue cannot cross function boundaries.
//│ ║  l.105: 	  let escape = () => root.continue
//│ ╙──       	                     ^^^^
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.


:effectHandlers
:lift
fun breakFromNestedFunctionWithEffects() =
  do root:
    fun escape() = root.break(42)
    escape()
breakFromNestedFunctionWithEffects()
//│ = 42


:e
:effectHandlers
:lift
fun breakFromNestedLambdaWithEffects() =
  do root:
    let escape = () => root.break(42)
    escape()
breakFromNestedLambdaWithEffects()
//│ ╔══[COMPILATION ERROR] Label break cannot cross lambda or handler boundaries.
//│ ║  l.128: 	    let escape = () => root.break(42)
//│ ╙──       	                       ^^^^
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.
