:js

class Box()

:w
fun foo(x) = if x is @compile Box then "yes" else "no"
//│ ╔══[WARNING] Cannot compile `Box`,
//│ ║  l.6: 	fun foo(x) = if x is @compile Box then "yes" else "no"
//│ ║       	                      ^^^^^^^^^^^
//│ ╟── because it is a class.
//│ ║  l.3: 	class Box()
//│ ║       	      ^^^
//│ ╙── Note: only patterns can be compiled.

object do_not_compile

:w
fun foo(x) = if x is @do_not_compile Box then "yes" else "no"
//│ ╔══[WARNING] Unknown annotation on pattern
//│ ║  l.18: 	fun foo(x) = if x is @do_not_compile Box then "yes" else "no"
//│ ║        	                      ^^^^^^^^^^^^^^
//│ ╙── Note: only `@compile` is supported on patterns.

pattern Box' = Box

fun foo(x) = if x is @compile Box' then "yes" else "no"

:w
fun foo(x) = if x is @do_not_compile Box' then "yes" else "no"
//│ ╔══[WARNING] Unknown annotation on pattern
//│ ║  l.29: 	fun foo(x) = if x is @do_not_compile Box' then "yes" else "no"
//│ ║        	                      ^^^^^^^^^^^^^^
//│ ╙── Note: only `@compile` is supported.
