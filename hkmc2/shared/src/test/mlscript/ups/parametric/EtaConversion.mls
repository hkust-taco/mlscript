:js

pattern Nullable(pattern T) = T | null

// Using patterns as pattern argument directly should not create local objects.

pattern Zero = 0

:ucs normalized
0 is Nullable(pattern Zero)
//│ Normalized:
//│ >  if
//│ >    let $scrut = 0
//│ >    let $param0 = member:Zero
//│ >    let $matchResult = (member:Nullable.)unapply($param0, $scrut)
//│ >    $matchResult is $runtime.MatchResult.class then true
//│ >    else false
//│ = true

import "../../../mlscript-compile/Char.mls"

:ucs normalized
fun foo(x) = x is Nullable(pattern Char.Letter)
//│ Normalized:
//│ >  if
//│ >    let $param0 = (member:Char.)Letter‹member:Letter›
//│ >    let $matchResult = (member:Nullable.)unapply($param0, x)
//│ >    $matchResult is $runtime.MatchResult.class then true
//│ >    else false

foo of null
//│ = true

foo of "A"
//│ = true
