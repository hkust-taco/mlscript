:js




:deadParamElim
module M with
  fun f(a) = 0
  fun g(b) = b
M.f(1)
M.g(2)
//│ dead-param-elim > >>> dead-param-elim results >>>
//│ dead-param-elim > prodfun f#0 @ f@2 -> eliminable: {0}
//│ dead-param-elim > <<< dead-param-elim results <<<
//│ = 2


:deadParamElim
:expect 0
module M with
  fun f(a) = 0
  fun main() = M.f(42)
M.main()
//│ dead-param-elim > >>> dead-param-elim results >>>
//│ dead-param-elim > prodfun f#0 @ main@4.f@1 -> eliminable: {0}
//│ dead-param-elim > prodfun f#0 @ main@6.f@1 -> eliminable: {0}
//│ dead-param-elim > <<< dead-param-elim results <<<
//│ = 0


:deadParamElim
:expect 42
module M with
  fun f(a) = a
  fun main() = M.f(42)
M.main()
//│ dead-param-elim > >>> dead-param-elim results >>>
//│ dead-param-elim > <<< dead-param-elim results <<<
//│ = 42




:deadParamElim
module M with
  fun f(a) = a
  fun g(b) = 0
  fun pick(x)(y) = if false then x else y
  fun main() = M.pick(M.f)(M.g)(5)
M.main()
//│ dead-param-elim > >>> dead-param-elim results >>>
//│ dead-param-elim > <<< dead-param-elim results <<<
//│ = 0
