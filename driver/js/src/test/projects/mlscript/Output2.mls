import "json5"

class CompilerOptions(outDir: string)
class TSConfig(compilerOptions: CompilerOptions)

fun createConfig(path: string) =
  let options = { outDir: path }
  let config = { compilerOptions: options }
  json5.stringify(config)

let config = createConfig("bar")
log(config)
