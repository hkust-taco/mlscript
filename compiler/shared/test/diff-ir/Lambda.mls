:NewParser
:ParseOnly
:UseIR

:interpIR
:genCpp
// fun local_rec_lambda() =
//   let rec f = x => if x == 0 then 0 else f(x - 1)
// 
//   class Lambda$0 extends Callable {
//     fun apply1(x) = if x == 0 then 0 else f(x - 1)
//   }
//   let rec f = Lmabda$0(...)
// 
//   let rec f =
//     class Lambda$0 extends Callable {
//       fun apply1(x) = if x == 0 then 0 else f(x - 1)
//     }
//     Lmabda$0(...)
// 
//   f(10)
// local_rec_lambda()
// fun local_rec_lambda() =
//   let rec f =
//     class Lambda$0() {
//       fun apply1(x) = if x == 0 then 0 else f(x - 1)
//     }
//     Lmabda$0()
//   f(10)
// local_rec_lambda()
// class Lambda$0(f) {
//   fun apply1(x) = if x == 0 then 0 else f(x - 1)
// }
// fun local_rec_lambda() =
//   let rec f =
//     Lmabda$0(f)
//   f(10)
// local_rec_lambda()
